apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: airport-platform
data:
  APP_ENV: "development"
  APP_VERSION: "1.0"
  DB_HOST: "postgres-service"
  DB_NAME: "passengerdb"
  DB_PORT: "5432"
