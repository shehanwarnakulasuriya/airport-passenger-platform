apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: airport-platform
  labels:
    app: backend-api
spec:
  type: ClusterIP
  selector:
    app: backend-api
  ports:
    - name: http
      port: 5000
      targetPort: 5000
